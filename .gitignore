# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
dist/
build/
.mypy_cache/
.ruff_cache/
.pytest_cache/
htmlcov/
.coverage

# Config / secrets
.env
config.yaml
config.yaml.bak
config.yaml.tmp

# Data (Docker volumes)
store/
data/

# Local CI build cache (scripts/ci-local)
.buildcache/

# Node
ui/node_modules/
ui/dist/

# OS
.DS_Store
Thumbs.db

# AI assistant data. CLAUDE.md and .claude/{rules,skills}/ are intentionally committed —
# they are the repo's working agreement, standards, and shared skills, enforced for everyone
# and carried with the project wherever it's cloned. Only personal/local settings stay out.
.vscode/launch.json
.vscode/settings.json
.playwright-mcp/
.claude/settings.local.json
