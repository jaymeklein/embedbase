# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
dist/
build/
.mypy_cache/
.ruff_cache/
.pytest_cache/
htmlcov/
.coverage

# Config / secrets
.env
config.yaml
config.yaml.bak
config.yaml.tmp

# Data (Docker volumes)
store/
data/

# Local CI build cache (scripts/ci-local)
.buildcache/

# Node
ui/node_modules/
ui/dist/

# OS
.DS_Store
Thumbs.db

# AI assistant local session data. CLAUDE.md is intentionally committed — it is the
# repo's working agreement / coding standard, enforced for everyone (see the file).
.claude/*
.vscode/launch.json
.vscode/settings.json
.playwright-mcp/
