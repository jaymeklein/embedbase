# Worker runtime dependencies (light) — messaging, config, lightweight search.
# Heavy ML/ingestion deps (torch, chromadb, parsers, vector-store drivers) live
# in worker/requirements-ml.txt and install in a separate Docker layer gated by
# the INSTALL_ML build-arg, so changes here never trigger a torch reinstall.
celery[redis]>=5.4.0
redis>=5.0.0
watchfiles>=0.13          # hot-reload wrapper for dev (replaces removed --autoreload)
structlog>=24.1.0
pydantic>=2.7.0
pydantic-settings>=2.2.0
PyYAML>=6.0.1
# Shared DB schema (sync engine over the same SQLite file)
sqlalchemy[asyncio]>=2.0.0

# Lightweight ingestion helpers (tokenisation, BM25, encoding + markdown parsing)
tiktoken>=0.7.0
rank-bm25>=0.2.2
chardet>=5.2.0
mistune>=3.0.0
