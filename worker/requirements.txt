# Delivery 1 — minimal runtime deps (worker tasks are stubs)
# Full parser/embedding deps restored in Delivery 2
celery[redis]>=5.4.0
redis>=5.0.0
structlog>=24.1.0
pydantic>=2.7.0
pydantic-settings>=2.2.0
PyYAML>=6.0.1
aiosqlite>=0.20.0
bcrypt>=4.1.0
